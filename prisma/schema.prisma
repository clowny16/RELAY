generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// No database models — RELAY is a fully client-side app.
// All conversions run in the browser; no user data is stored server-side.
